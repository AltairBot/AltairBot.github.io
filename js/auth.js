const Auth = { initGlobalUI: function () { const logo = document.querySelector(".logo"); if (logo) { logo.style.cursor = "pointer"; logo.addEventListener("click", () => { const path = window.location.pathname; const mainPages = ["index.html", "features.html", "commands.html", "premium.html"]; const isMainPage = mainPages.some(page => path.includes(page)) || path === "/" || path.endsWith("/"); if (isMainPage) { window.scrollTo({ top: 0, behavior: "smooth" }); } else { window.location.href = "index.html"; } }); } } }; document.addEventListener("DOMContentLoaded", () => { Auth.initGlobalUI(); });
